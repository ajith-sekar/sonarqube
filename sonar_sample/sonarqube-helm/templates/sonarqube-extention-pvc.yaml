apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarqube-extension-pvc
  namespace: sonarqube
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: sonarqube-extension-sc
